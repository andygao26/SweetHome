@model System.Collections.Generic.Dictionary<string, string>
@{
    ViewData["Title"] = "ECPay";
    
}
<!DOCTYPE html>
<html>
    <head>
        <style>
            .h-600px{
                height:600px;
            }

        </style>
    </head>
<body>
    <div class="d-flex justify-content-center ">
        <h2 class="h-600px">轉跳中請稍後</h2>
    </div>
    <form hidden id="form" name="form" method="POST" action="https://payment-stage.ecpay.com.tw/Cashier/AioCheckOut/V5">
        <!--step2 : 收到後端的值印出來-->
        @foreach (var key in @Model.Keys.ToList())
        {
            @(key) <input type="text" name="@key" value="@Model[key]" />

            <br />
        }
    </form>
    <!-- js套件 -->
    <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/5.1.3/js/bootstrap.bundle.min.js"></script>
    <script src="https://code.jquery.com/jquery-3.7.1.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/jquery-twzipcode@1.7.14/jquery.twzipcode.min.js"></script>
    <!-- 自己的 -->
    <script>



        $(document).ready(function () {

            let formData = $("#form").serializeArray();
            var json = {};
            $.each(formData, function () {
                json[this.name] = this.value || "";
            });
            console.log(json);
            //step3 : 新增訂單到資料庫
            $.ajax({
                type: 'POST',
                url: 'https://silencehouse2905.azurewebsites.net/api/ECPay/AddOrders',
                contentType: 'application/json; charset=utf-8',
                data: JSON.stringify(json),
                success: function (res) {
                    console.log(res);

                },
                error: function (err) { console.log(err); },
            });
            $('#form').submit();
        });
   

        //$("#checkoutBtn").on('click', (e) => {
        //    // e.preventDefault(); //因為送出就跳轉到綠界，這個可以停住確認自己的console.log的內容
        //    let formData = $("#form").serializeArray();
        //    var json = {};
        //    $.each(formData, function () {
        //        json[this.name] = this.value || "";
        //    });
        //    console.log(json);
        //    //step3 : 新增訂單到資料庫
        //    $.ajax({
        //        type: 'POST',
        //        url: 'https://5831-111-255-207-229.ngrok-free.app/api/ECPay/AddOrders',
        //        contentType: 'application/json; charset=utf-8',
        //        data: JSON.stringify(json),
        //        success: function (res) {
        //            console.log(res);
                   
        //        },
        //        error: function (err) { console.log(err); },
        //    });
        //});

    </script>
</body>
</html>